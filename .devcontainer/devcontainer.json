{
  "name": "LA County Topo Dev Environment",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y pdal libpdal-dev gdal-bin libgdal-dev && pip install -r requirements.txt",
  "forwardPorts": [8000]
}